'use strict';

function CalculateFogIndex(text) {
    

    // Set up the return data
    var data = {
        index: 0,
    }

    return data;
}

/**
 * Export
 */
module.exports = CalculateFogIndex;
